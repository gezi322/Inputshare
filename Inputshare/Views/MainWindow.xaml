<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:Inputshare.ViewModels;assembly=Inputshare"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
        x:Class="Inputshare.Views.MainWindow"
        Icon="/Assets/avalonia-logo.ico"
        Title="Inputshare">

    <Design.DataContext>
        <vm:MainWindowViewModel/>
    </Design.DataContext>

  <DockPanel>
    <ContentControl Content="{Binding CurrentView}" DockPanel.Dock="Top" VerticalAlignment="Stretch"/>
    <Button Content="{Binding CurrentView.BottomButtonText}" Command="{Binding HandleBottomButtonPressAsync}" DockPanel.Dock="Bottom" Height="25" VerticalAlignment="Bottom"/>
  </DockPanel>
</Window>
